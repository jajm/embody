To build and install the library, run the following commands:

	autoconf
	./configure
	make
	make install # as root

Alternatively, if you're on a Debian system, you can build the debian packages:

	debuild -uc -us

and install them with dpkg.
